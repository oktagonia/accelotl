module requantize
  #(parameter WIDTH = 8,
    parameter NEURONS = 3,
    parameter SHIFT_WIDTH = 5,
    parameter OUT_WIDTH = 2*WIDTH + $clog2(NEURONS))
   (input logic                          clk,
    
    logic [SHIFT_WIDTH-1:0]              shift,
    logic signed [NEURONS*OUT_WIDTH-1:0] activations,
    logic signed [NEURONS*WIDTH-1:0]     out);


   
endmodule // requantize
